Name: Configure ChatKit
Instructions:
Setup OpenAI ChatKit in Next.js:
- Add domain allowlist key to env.
- Build chat UI component with message history.
- POST to /api/{user_id}/chat endpoint with conversation_id.
- Display history, tool calls visually (as cards).
- Show typing/loading state during agent run.
- Integrate Better Auth session (auto-include JWT).
- Handle conversation resume and new chat.
- Mobile-responsive, clean Tailwind design.

