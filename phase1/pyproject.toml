[project]
name = "console-todo-app"
version = "0.1.0"
description = "Phase I: Console Todo App - Python CLI with in-memory storage"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
